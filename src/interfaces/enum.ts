export enum EStatus {
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE'
}

export enum GameType {
  SOLO = 'solo',
  SUMMARY = 'summary'
}

export enum ETypeRoom {
  PRIVATE = 'PRIVATE',
  PUBLIC = 'PUBLIC',
  DRAF = 'DRAF'
}

export enum EGameInfo {
  ROOM = 'room',
  TYPE = 'type'
}

export enum EGameMutiple {
  CHARTS = 'charts',
  QUESTIONS = 'questions'
}
